# Nixpacks configuration for Railway deployment
[phases.setup]
cmds = ["echo 'No setup needed'"]

[phases.install]
cmds = ["npm ci --only=production"]

[phases.build]
cmds = ["echo 'Using pre-built files - no build needed'"]

[phases.start]
cmd = "npm start"

